import pymongo
import random

client = pymongo.MongoClient("mongodb://localhost:27017/")
pva_db = client["pva"]

water_col = pva_db['water']

# many = [{'value': random.randint(1, 100)} for _ in range(100_000)]
# x = water_col.insert_many(many)

y = water_col.aggregate([{'$avg': 'value'}])

print(len(list(y)))


print(client.list_database_names())